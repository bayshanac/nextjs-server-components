/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'homepageacademy.rs', 'coca-colapodrskamladima.com'],
  },
}

module.exports = nextConfig
